[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn mighty_server:app --bind 0.0.0.0:$PORT --workers 4 --threads 2 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
